toolsets:
  kubernetes/logs:
    enabled: false  # Disable to force use of Loki
  kubernetes/core:
    enabled: true
  grafana/loki:
    enabled: true
    config:
      url: http://localhost:3100
      api_key: ""  # No auth needed for local Loki
