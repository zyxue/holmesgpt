toolsets:

  datadog/traces:
    enabled: true
    config:
      # These will be populated from environment variables
      dd_api_key: "{{env.DATADOG_API_KEY}}"
      dd_app_key: "{{env.DATADOG_APP_KEY}}"
      site_api_url: "https://api.datadoghq.eu"  # EU endpoint to match our API keys
