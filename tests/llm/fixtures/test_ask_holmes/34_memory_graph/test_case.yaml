user_prompt: "Give me a graph of `container_memory_working_set_bytes`"
expected_output:
  - 'The answer should include something similar to << { "type": "promql", "tool_name": "execute_prometheus_range_query", "tool_call_id": "<alphanumeric_characters>" } >>. Both tool_name and tool_call_id MUST absolutely be present and the same as mentioned here.'
tags:
  - prometheus
  - embeds
evaluation:
  correctness: 1
port_forwards:
  - namespace: default
    service: robusta-kube-prometheus-st-prometheus
    local_port: 10034
    remote_port: 9090
