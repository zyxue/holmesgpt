# Secret is created in test_case.yaml setup from ../coralogix_app.py
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend-service
  template:
    metadata:
      labels:
        app: frontend-service
    spec:
      containers:
      - name: frontend-service
        image: python:3.9-slim
        command:
        - sh
        - -c
        - |
          pip install flask opentelemetry-api opentelemetry-sdk opentelemetry-exporter-otlp-proto-http opentelemetry-instrumentation --quiet
          python /app/app.py
        volumeMounts:
        - name: app
          mountPath: /app
        ports:
        - containerPort: 8080
        env:
        - name: PYTHONUNBUFFERED
          value: "1"
        - name: CORALOGIX_LOGS_ENABLED
          value: "false"
        - name: CORALOGIX_TRACES_ENABLED
          value: "false"
        - name: CORALOGIX_METRICS_ENABLED
          value: "true"
        - name: SERVICE_NAME
          value: "frontend-service"
        - name: SERVICE_LABEL
          value: "frontend"
        - name: K8S_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: K8S_POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: K8S_NODE_IP
          valueFrom:
            fieldRef:
              fieldPath: status.hostIP
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "500m"
      volumes:
      - name: app
        secret:
          secretName: frontend-service-app
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: frontend-service
  ports:
  - port: 8080
    targetPort: 8080
